document.addEventListener('DOMContentLoaded', () => { const novos = document.getElementById('novos'); const andamento = document.getElementById('andamento'); const concluidos = document.getElementById('concluidos'); // Simulação de números dinâmicos novos.textContent = Math.floor(Math.random() * 50) + 1; andamento.textContent = Math.floor(Math.random() * 100) + 10; concluidos.textContent = Math.floor(Math.random() * 200) + 50; }); // Formulário de Contato - Geração Automática de Processo document.getElementById('form-contato').addEventListener('submit', (event) => { event.preventDefault(); const nome = document.getElementById('nome').value; const email = document.getElementById('email').value; const mensagem = document.getElementById('mensagem').value; // Geração de número de processo único const numeroProcesso = `PROC-${Date.now().toString().slice(-6)}`; alert(`Obrigado, ${nome}! Seu número de processo é: ${numeroProcesso}`); // Simulação de envio via e-mail, WhatsApp e SMS enviarNotificacoes(nome, email, numeroProcesso); // Limpar o formulário document.getElementById('form-contato').reset(); }); // Função para simular envio de notificações function enviarNotificacoes(nome, email, numeroProcesso) { console.log(`E-mail enviado para ${email}: Seu número de processo é ${numeroProcesso}.`); console.log(`WhatsApp enviado: Olá ${nome}, seu processo foi criado com o número ${numeroProcesso}.`); console.log(`SMS enviado: Número do processo ${numeroProcesso}.`); }




