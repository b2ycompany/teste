document.addEventListener("DOMContentLoaded", function() { // Função para alterar a imagem de fundo do cabeçalho function changeBackgroundImage() { const bgUrl = document.getElementById("bg-url").value; // Obtém o URL da imagem inserido pelo usuário if (bgUrl) { // Alterando o fundo do cabeçalho document.getElementById("header").style.backgroundImage = `url(${bgUrl})`; } else { alert("Por favor, insira uma URL válida."); } } // Adiciona a funcionalidade para alterar a imagem de fundo do cabeçalho const changeBgButton = document.querySelector("button"); changeBgButton.addEventListener("click", changeBackgroundImage); // Função para mostrar um contador de serviços com animação let countStarted = false; const counters = document.querySelectorAll(".counter"); function startCounters() { if (countStarted) return; counters.forEach(counter => { const updateCounter = () => { const target = +counter.getAttribute("data-target"); const current = +counter.innerText; const increment = target / 100; if (current < target) { counter.innerText = Math.ceil(current + increment); setTimeout(updateCounter, 10); } else { counter.innerText = target; } }; updateCounter(); }); countStarted = true; } // Detecta quando a seção de contadores entra na tela para iniciar a contagem const countersSection = document.querySelector("#servicos"); const observer = new IntersectionObserver(entries => { if (entries[0].isIntersecting) { startCounters(); } }, { threshold: 0.5 }); observer.observe(countersSection); // Animação do menu ao passar o cursor const menuLinks = document.querySelectorAll(".menu a"); menuLinks.forEach(link => { link.addEventListener("mouseover", () => { link.style.color = "#fff"; link.style.transition = "color 0.3s ease"; }); link.addEventListener("mouseout", () => { link.style.color = "#d4af37"; }); }); // Efeito de transição suave para rolar até as seções const smoothScrollLinks = document.querySelectorAll('a[href^="#"]'); smoothScrollLinks.forEach(link => { link.addEventListener("click", (e) => { e.preventDefault(); const targetId = link.getAttribute("href"); const targetElement = document.querySelector(targetId); window.scrollTo({ top: targetElement.offsetTop - 60, behavior: "smooth" }); }); }); // Configuração inicial do contador e da imagem de fundo function init() { // Configurar imagem de fundo do cabeçalho const headerBackgroundUrl = document.getElementById("header").style.backgroundImage; if (headerBackgroundUrl === 'url("caminho-da-imagem-do-cabecalho.jpg")') { document.getElementById("header").style.backgroundImage = 'url("caminho-padrão.jpg")'; } } init(); });





