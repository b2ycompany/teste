// Função para atualizar os contadores de processos function updateCounters() { const novos = document.getElementById('novos'); const andamento = document.getElementById('andamento'); const concluidos = document.getElementById('concluidos'); // Valores fictícios para os contadores (estes valores podem ser dinâmicos conforme o banco de dados) const novosProcessos = 5; const processosAndamento = 3; const processosConcluidos = 8; // Atualiza o número dos contadores novos.textContent = novosProcessos; andamento.textContent = processosAndamento; concluidos.textContent = processosConcluidos; } // Função para animação do carrossel de depoimentos function startCarrossel() { const depoimentos = document.querySelectorAll('.depoimento'); let index = 0; setInterval(() => { // Esconde todos os depoimentos depoimentos.forEach((depoimento) => { depoimento.style.display = 'none'; }); // Exibe o próximo depoimento depoimentos[index].style.display = 'block'; // Atualiza o índice para o próximo depoimento index = (index + 1) % depoimentos.length; }, 3000); // Altere para a velocidade que você desejar } // Função para enviar o formulário de contato document.getElementById('form-contato').addEventListener('submit', function(event) { event.preventDefault(); // Simulação de um envio bem-sucedido const nome = document.getElementById('nome').value; const email = document.getElementById('email').value; const mensagem = document.getElementById('mensagem').value; const arquivo = document.getElementById('arquivo').files[0]; // Exibição de uma mensagem simples de sucesso alert('Formulário enviado com sucesso!'); // Resetar o formulário this.reset(); // Simular envio por e-mail (pode ser integrado com um serviço de backend) console.log('Enviado por e-mail: ', { nome, email, mensagem, arquivo }); // Simular envio via WhatsApp (pode ser integrado com uma API) const whatsappMessage = `Olá, gostaria de saber mais sobre os serviços. Nome: ${nome} E-mail: ${email} Mensagem: ${mensagem}`; window.open(`https://wa.me/5511999999999?text=${encodeURIComponent(whatsappMessage)}`); }); // Inicializando as funções document.addEventListener('DOMContentLoaded', () => { updateCounters(); // Atualiza os contadores de processos startCarrossel(); // Inicia o carrossel de depoimentos });




